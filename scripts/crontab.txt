# Sonata 热门板块自动更新 - Cron 配置
# 每 3 天执行一次（凌晨 3 点）

# 格式：分 时 日 月 周 命令

# 每 3 天凌晨 3 点执行
0 3 */3 * * cd /path/to/sonata-1.3 && uv run python scripts/update_sectors.py >> logs/cron_update.log 2>&1

# 或者使用 GitHub Actions（推荐）
# 见 .github/workflows/update-sectors.yml
